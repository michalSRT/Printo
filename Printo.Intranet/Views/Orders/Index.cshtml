@model IEnumerable<Printo.Data.Data.Order>
@using Microsoft.AspNetCore.Http;
@{
    ViewData["Title"] = "Zamówienia";
}

<div class="row">
    <div class="col-md-12">
        @*AKTYWNE*@
        <div class="card shadow">
            <div class="card-header">
                <div class="row">
                    <div class="col">
                        <h4 class="mb-2 page-title">Aktualne zamówienia</h4>
                    </div>
                    <div class="col">
                        @if (@Context.Session.GetString("UserTypeID") != "2")
                        {
                            <a class="btn btn-outline-success float-right" role="button" aria-haspopup="true" aria-expanded="false" asp-action="Create">
                                <i class="fe fe-plus fe-16" style="padding-right:5px; vertical-align:text-bottom"></i>
                                Dodaj
                            </a>
                        }
                    </div>
                </div>
            </div>
            <div class="row" style="place-content: center; margin-bottom: 15px;">
                <div class="col" style=" text-align: center; ">
                    <a class="btn btn-outline-secondary" style="margin:5px; role="button" aria-haspopup="true" aria-expanded="false" asp-action="Index">
                        Wszystkie
                    </a>
                    @foreach (var item in ViewBag.ProductionStage)
                    {
                        <a class="btn btn-outline-secondary" style="margin:5px; border-color:@item.Color" role="button" aria-haspopup="true" aria-expanded="false" asp-action="Index" asp-route-id="@item.ProductionStageID">
                            @item.Name
                        </a>
                    }
                </div>
            </div>
            @if (Model.Where(x => x.IsActive == true && x.MachineID == 2).FirstOrDefault() != null)
            {
                <div class="card-body">
                    <div class="row">
                        <div class="col">
                            <h4 class="mb-2 page-title">KBA</h4>
                        </div>
                    </div>
                    <table class="table dataTable table-responsive-lg table-hover" id="dataTable-orders-kba" style="zoom:0.75">
                        <thead>
                            <tr>
                                <th>Nr</th>
                                <th>Start</th>
                                <th>Koniec</th>
                                <th>Etap produkcji</th>
                                <th>Klient</th>
                                <th>Nazwa zamówienia</th>
                                <th>Produkt</th>
                                <th>Nakład</th>
                                <th>Cena</th>
                                <th style="width:200px">Akcja</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model.Where(x => x.IsActive == true && x.MachineID == 2))
                            {
                                <tr class="tableCursor" onclick="location.href = '@(Url.Action("Edit", "Orders", new { id = item.OrderID }, null))'">
                                    <td>
                                        @Html.DisplayFor(modelItem => item.OrderID)
                                    </td>
                                    <td style="text-align:center">
                                        @if (item.StartDate != null)
                                        {@item.StartDate.Value.ToString("dd/MM")}
                                    </td>
                                    <td style="text-align:center">
                                        @if (item.EndDate != null)
                                        {@item.EndDate.Value.ToString("dd/MM")}
                                    </td>
                                    <td>
                                        @if (item.ProductionStage.Color != null)
                                        {
                                            @if (item.ProductionStage.Color.ToString() == "#ffffff")
                                            {
                                                <a class="btn disabled" type="button" style="padding-bottom:2px;color:black;background:@Html.DisplayFor(modelItem => item.ProductionStage.Color)">
                                                    @Html.DisplayFor(modelItem => item.ProductionStage.Name)
                                                </a>
                                            }
                                            else
                                            {
                                                <a class="btn disabled" type="button" style="padding-bottom:2px;color:white;background:@Html.DisplayFor(modelItem => item.ProductionStage.Color)">
                                                    @Html.DisplayFor(modelItem => item.ProductionStage.Name)
                                                </a>
                                            }
                                        }
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Client.Name)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.OrderName)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Product.Name)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Quantity)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.NetPrice)
                                    </td>
                                    <td>
                                        <a class="btn btn-outline-success" style="margin-bottom:1px" asp-action="Edit" asp-route-id="@item.OrderID" role="button" data-toggle="tooltip" title="Edytuj">
                                            <i class="fe fe-edit fe-16"></i>
                                        </a>
                                        <a class="btn btn-outline-info" style="margin-bottom:1px" asp-action="ChangeProductionStage" asp-route-id="@item.OrderID" asp-route-prodId="3" role="button" data-toggle="tooltip" title="Do druku">
                                            <i class="fe fe-printer fe-16"></i>
                                        </a>                                        
                                        <a class="btn btn-outline-dark" style="margin-bottom:1px" asp-action="ChangeProductionStage" asp-route-id="@item.OrderID" asp-route-prodId="7" role="button" data-toggle="tooltip" title="Gotowe">
                                            <i class="fe fe-check fe-16"></i>
                                        </a>
                                        <a class="btn btn-outline-dark" style="margin-bottom:1px" asp-action="ChangeProductionStage" asp-route-id="@item.OrderID" asp-route-prodId="8" role="button" data-toggle="tooltip" title="Archiwum">
                                            <i class="fe fe-archive fe-16"></i>
                                        </a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }
            @if (Model.Where(x => x.IsActive == true && x.MachineID == 4).FirstOrDefault() != null)
            {
                <div class="card-body">
                    <div class="row">
                        <div class="col">
                            <h4 class="mb-2 page-title">LASER</h4>
                        </div>
                    </div>
                    <table class="table dataTable table-responsive-lg table-hover" id="dataTable-orders-laser" style="zoom:0.75">
                        <thead>
                            <tr>
                                <th>Nr</th>
                                <th>Start</th>
                                <th>Koniec</th>
                                <th>Etap produkcji</th>
                                <th>Klient</th>
                                <th>Nazwa zamówienia</th>
                                <th>Produkt</th>
                                <th>Nakład</th>
                                <th>Cena</th>
                                <th style="width:200px">Akcja</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model.Where(x => x.IsActive == true && x.MachineID == 4))
                            {
                                <tr class="tableCursor" onclick="location.href = '@(Url.Action("Edit", "Orders", new { id = item.OrderID }, null))'">
                                    <td>
                                        @Html.DisplayFor(modelItem => item.OrderID)
                                    </td>
                                    <td style="text-align:center">
                                        @if (item.StartDate != null)
                                        {@item.StartDate.Value.ToString("dd/MM")}
                                    </td>
                                    <td style="text-align:center">
                                        @if (item.EndDate != null)
                                        {@item.EndDate.Value.ToString("dd/MM")}
                                    </td>
                                    <td>
                                        @if (item.ProductionStage.Color != null)
                                        {
                                            @if (item.ProductionStage.Color.ToString() == "#ffffff")
                                            {
                                                <a class="btn disabled" type="button" style="padding-bottom:2px;color:black;background:@Html.DisplayFor(modelItem => item.ProductionStage.Color)">
                                                    @Html.DisplayFor(modelItem => item.ProductionStage.Name)
                                                </a>
                                            }
                                            else
                                            {
                                                <a class="btn disabled" type="button" style="padding-bottom:2px;color:white;background:@Html.DisplayFor(modelItem => item.ProductionStage.Color)">
                                                    @Html.DisplayFor(modelItem => item.ProductionStage.Name)
                                                </a>
                                            }
                                        }
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Client.Name)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.OrderName)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Product.Name)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Quantity)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.NetPrice)
                                    </td>
                                    <td>
                                        <a class="btn btn-outline-success" style="margin-bottom:1px" asp-action="Edit" asp-route-id="@item.OrderID" role="button" data-toggle="tooltip" title="Edytuj">
                                            <i class="fe fe-edit fe-16"></i>
                                        </a>
                                        <a class="btn btn-outline-info" style="margin-bottom:1px" asp-action="ChangeProductionStage" asp-route-id="@item.OrderID" asp-route-prodId="3" role="button" data-toggle="tooltip" title="Do druku">
                                            <i class="fe fe-printer fe-16"></i>
                                        </a>
                                        <a class="btn btn-outline-dark" style="margin-bottom:1px" asp-action="ChangeProductionStage" asp-route-id="@item.OrderID" asp-route-prodId="7" role="button" data-toggle="tooltip" title="Gotowe">
                                            <i class="fe fe-check fe-16"></i>
                                        </a>
                                        <a class="btn btn-outline-dark" style="margin-bottom:1px" asp-action="ChangeProductionStage" asp-route-id="@item.OrderID" asp-route-prodId="8" role="button" data-toggle="tooltip" title="Archiwum">
                                            <i class="fe fe-archive fe-16"></i>
                                        </a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }
            @if (Model.Where(x => x.IsActive == true && x.MachineID == 3).FirstOrDefault() != null)
            {
                <div class="card-body">
                    <div class="row">
                        <div class="col">
                            <h4 class="mb-2 page-title">RYOBI</h4>
                        </div>
                    </div>
                    <table class="table dataTable table-responsive-lg table-hover" id="dataTable-orders-ryobi" style="zoom:0.75">
                        <thead>
                            <tr>
                                <th>Nr</th>
                                <th>Start</th>
                                <th>Koniec</th>
                                <th>Etap produkcji</th>
                                <th>Klient</th>
                                <th>Nazwa zamówienia</th>
                                <th>Produkt</th>
                                <th>Nakład</th>
                                <th>Cena</th>
                                <th style="width:200px">Akcja</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model.Where(x => x.IsActive == true && x.MachineID == 3))
                            {
                                <tr class="tableCursor" onclick="location.href = '@(Url.Action("Edit", "Orders", new { id = item.OrderID }, null))'">
                                    <td>
                                        @Html.DisplayFor(modelItem => item.OrderID)
                                    </td>
                                    <td style="text-align:center">
                                        @if (item.StartDate != null)
                                        {@item.StartDate.Value.ToString("dd/MM")}
                                    </td>
                                    <td style="text-align:center">
                                        @if (item.EndDate != null)
                                        {@item.EndDate.Value.ToString("dd/MM")}
                                    </td>
                                    <td>
                                        @if (item.ProductionStage.Color != null)
                                        {
                                            @if (item.ProductionStage.Color.ToString() == "#ffffff")
                                            {
                                                <a class="btn disabled" type="button" style="padding-bottom:2px;color:black;background:@Html.DisplayFor(modelItem => item.ProductionStage.Color)">
                                                    @Html.DisplayFor(modelItem => item.ProductionStage.Name)
                                                </a>
                                            }
                                            else
                                            {
                                                <a class="btn disabled" type="button" style="padding-bottom:2px;color:white;background:@Html.DisplayFor(modelItem => item.ProductionStage.Color)">
                                                    @Html.DisplayFor(modelItem => item.ProductionStage.Name)
                                                </a>
                                            }
                                        }
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Client.Name)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.OrderName)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Product.Name)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Quantity)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.NetPrice)
                                    </td>
                                    <td>
                                        <a class="btn btn-outline-success" style="margin-bottom:1px" asp-action="Edit" asp-route-id="@item.OrderID" role="button" data-toggle="tooltip" title="Edytuj">
                                            <i class="fe fe-edit fe-16"></i>
                                        </a>
                                        <a class="btn btn-outline-info" style="margin-bottom:1px" asp-action="ChangeProductionStage" asp-route-id="@item.OrderID" asp-route-prodId="3" role="button" data-toggle="tooltip" title="Do druku">
                                            <i class="fe fe-printer fe-16"></i>
                                        </a>
                                        <a class="btn btn-outline-dark" style="margin-bottom:1px" asp-action="ChangeProductionStage" asp-route-id="@item.OrderID" asp-route-prodId="7" role="button" data-toggle="tooltip" title="Gotowe">
                                            <i class="fe fe-check fe-16"></i>
                                        </a>
                                        <a class="btn btn-outline-dark" style="margin-bottom:1px" asp-action="ChangeProductionStage" asp-route-id="@item.OrderID" asp-route-prodId="8" role="button" data-toggle="tooltip" title="Archiwum">
                                            <i class="fe fe-archive fe-16"></i>
                                        </a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }
            @if (Model.Where(x => x.IsActive == true && x.MachineID == 1).FirstOrDefault() != null)
            {
                <div class="card-body">
                    <div class="row">
                        <div class="col">
                            <h4 class="mb-2 page-title">Nieprzydzielone</h4>
                        </div>
                    </div>
                    <table class="table dataTable table-responsive-lg table-hover" id="dataTable-orders-nd" style="zoom:0.75">
                        <thead>
                            <tr>
                                <th>Nr</th>
                                <th>Start</th>
                                <th>Koniec</th>
                                <th>Etap produkcji</th>
                                <th>Klient</th>
                                <th>Nazwa zamówienia</th>
                                <th>Produkt</th>
                                <th>Nakład</th>
                                <th>Cena</th>
                                <th style="width:200px">Akcja</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model.Where(x => x.IsActive == true && x.MachineID == 1))
                            {
                                <tr class="tableCursor" onclick="location.href = '@(Url.Action("Edit", "Orders", new { id = item.OrderID }, null))'">
                                    <td>
                                        @Html.DisplayFor(modelItem => item.OrderID)
                                    </td>
                                    <td style="text-align:center">
                                        @if (item.StartDate != null)
                                        {@item.StartDate.Value.ToString("dd/MM")}
                                    </td>
                                    <td style="text-align:center">
                                        @if (item.EndDate != null)
                                        {@item.EndDate.Value.ToString("dd/MM")}
                                    </td>
                                    <td>
                                        @if (item.ProductionStage.Color != null)
                                        {
                                            @if (item.ProductionStage.Color.ToString() == "#ffffff")
                                            {
                                                <a class="btn disabled" type="button" style="padding-bottom:2px;color:black;background:@Html.DisplayFor(modelItem => item.ProductionStage.Color)">
                                                    @Html.DisplayFor(modelItem => item.ProductionStage.Name)
                                                </a>
                                            }
                                            else
                                            {
                                                <a class="btn disabled" type="button" style="padding-bottom:2px;color:white;background:@Html.DisplayFor(modelItem => item.ProductionStage.Color)">
                                                    @Html.DisplayFor(modelItem => item.ProductionStage.Name)
                                                </a>
                                            }
                                        }
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Client.Name)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.OrderName)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Product.Name)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Quantity)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.NetPrice)
                                    </td>
                                    <td>
                                        <a class="btn btn-outline-success" style="margin-bottom:1px" asp-action="Edit" asp-route-id="@item.OrderID" role="button" data-toggle="tooltip" title="Edytuj">
                                            <i class="fe fe-edit fe-16"></i>
                                        </a>
                                        <a class="btn btn-outline-info" style="margin-bottom:1px" asp-action="ChangeProductionStage" asp-route-id="@item.OrderID" asp-route-prodId="3" role="button" data-toggle="tooltip" title="Do druku">
                                            <i class="fe fe-printer fe-16"></i>
                                        </a>
                                        <a class="btn btn-outline-dark" style="margin-bottom:1px" asp-action="ChangeProductionStage" asp-route-id="@item.OrderID" asp-route-prodId="7" role="button" data-toggle="tooltip" title="Gotowe">
                                            <i class="fe fe-check fe-16"></i>
                                        </a>
                                        <a class="btn btn-outline-dark" style="margin-bottom:1px" asp-action="ChangeProductionStage" asp-route-id="@item.OrderID" asp-route-prodId="8" role="button" data-toggle="tooltip" title="Archiwum">
                                            <i class="fe fe-archive fe-16"></i>
                                        </a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }
            @if (Model.Where(x => x.IsActive == true).FirstOrDefault() != null)
            {
                <div class="card-body">
                    <div class="row">
                        <div class="col">
                            <h4 class="mb-2 page-title">WSZYSTKIE ZAMÓWIENIA</h4>
                        </div>
                    </div>
                    <table class="table dataTable table-responsive-lg table-hover" id="dataTable-orders-kba" style="zoom:0.75">
                        <thead>
                            <tr>
                                <th>Nr</th>
                                <th>Start</th>
                                <th>Koniec</th>
                                <th>Maszyna</th>
                                <th>Etap produkcji</th>
                                <th>Klient</th>
                                <th>Nazwa zamówienia</th>
                                <th>Produkt</th>
                                <th>Nakład</th>
                                <th>Cena</th>
                                <th style="width:200px">Akcja</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model.Where(x => x.IsActive == true && x.MachineID == 2))
                            {
                                <tr class="tableCursor" onclick="location.href = '@(Url.Action("Edit", "Orders", new { id = item.OrderID }, null))'">
                                    <td>
                                        @Html.DisplayFor(modelItem => item.OrderID)
                                    </td>
                                    <td style="text-align:center">
                                        @if (item.StartDate != null)
                                        {@item.StartDate.Value.ToString("dd/MM")}
                                    </td>
                                    <td style="text-align:center">
                                        @if (item.EndDate != null)
                                        {@item.EndDate.Value.ToString("dd/MM")}
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Machine.Name)
                                    </td>
                                    <td>
                                        @if (item.ProductionStage.Color != null)
                                        {
                                            @if (item.ProductionStage.Color.ToString() == "#ffffff")
                                            {
                                                <a class="btn disabled" type="button" style="padding-bottom:2px;color:black;background:@Html.DisplayFor(modelItem => item.ProductionStage.Color)">
                                                    @Html.DisplayFor(modelItem => item.ProductionStage.Name)
                                                </a>
                                            }
                                            else
                                            {
                                                <a class="btn disabled" type="button" style="padding-bottom:2px;color:white;background:@Html.DisplayFor(modelItem => item.ProductionStage.Color)">
                                                    @Html.DisplayFor(modelItem => item.ProductionStage.Name)
                                                </a>
                                            }
                                        }
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Client.Name)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.OrderName)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Product.Name)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Quantity)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.NetPrice)
                                    </td>
                                    <td>
                                        <a class="btn btn-outline-success" style="margin-bottom:1px" asp-action="Edit" asp-route-id="@item.OrderID" role="button" data-toggle="tooltip" title="Edytuj">
                                            <i class="fe fe-edit fe-16"></i>
                                        </a>
                                        <a class="btn btn-outline-info" style="margin-bottom:1px" asp-action="ChangeProductionStage" asp-route-id="@item.OrderID" asp-route-prodId="3" role="button" data-toggle="tooltip" title="Do druku">
                                            <i class="fe fe-printer fe-16"></i>
                                        </a>
                                        <a class="btn btn-outline-dark" style="margin-bottom:1px" asp-action="ChangeProductionStage" asp-route-id="@item.OrderID" asp-route-prodId="7" role="button" data-toggle="tooltip" title="Gotowe">
                                            <i class="fe fe-check fe-16"></i>
                                        </a>
                                        <a class="btn btn-outline-dark" style="margin-bottom:1px" asp-action="ChangeProductionStage" asp-route-id="@item.OrderID" asp-route-prodId="8" role="button" data-toggle="tooltip" title="Archiwum">
                                            <i class="fe fe-archive fe-16"></i>
                                        </a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }

        </div>
        @*NIEAKTYWNE*@
        @foreach (var item in Model)
        {
            if (item.IsActive == false)
            {
                <div class="row">
                    <div class="col" style="text-align:center; margin:10px">
                        <button class="btn btn-outline-dark" type="button" data-toggle="collapse" data-target="#collapseExample" aria-expanded="false" aria-controls="collapseExample">
                            <i class="fe fe-trash-2 fe-16"></i> Kosz
                        </button>
                    </div>
                </div>
                break;
            }
        }
        <div class="card shadow collapse" id="collapseExample">
            <div class="card-header">
                <div class="row">
                    <div class="col">
                        <h4 class="mb-2 page-title">Zamówienia - pozycje nieaktywne</h4>
                    </div>
                </div>
            </div>
            <div class="card-body">
                <table class="table dataTable table-responsive-lg table-hover" id="dataTable-2">
                    <thead>
                        <tr>
                            <th>Nr</th>
                            <th>Start</th>
                            <th>Koniec</th>
                            <th>Etap produkcji</th>
                            <th>Klient</th>
                            <th>Nazwa zamówienia</th>
                            <th>Produkt</th>
                            <th>Nakład</th>
                            <th style="width:73px">Akcja</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model.Where(x => x.IsActive == false))
                        {
                            <tr class="tableCursor" onclick="location.href = '@(Url.Action("Edit", "Orders", new { id = item.OrderID }, null))'">
                                <td>
                                    @Html.DisplayFor(modelItem => item.OrderID)
                                </td>
                                <td style="text-align:center">
                                    @if (item.StartDate != null)
                                    {@item.StartDate.Value.ToString("dd/MM")}
                                    <br />
                                    @if (item.StartDate != null)
                                    {@item.StartDate.Value.ToString("H:mm")}
                                </td>
                                <td style="text-align:center">
                                    @if (item.StartDate != null)
                                    {@item.StartDate.Value.ToString("dd/MM")}
                                    <br />
                                    @if (item.StartDate != null)
                                    {@item.StartDate.Value.ToString("H:mm")}
                                </td>
                                <td>
                                    @if (item.ProductionStage.Color != null)
                                    {
                                        @if (item.ProductionStage.Color.ToString() == "#ffffff" || item.ProductionStage.Color.ToString() == null)
                                        {
                                            <a class="btn disabled" type="button" style="padding-bottom:2px;color:black;background:@Html.DisplayFor(modelItem => item.ProductionStage.Color)">
                                                @Html.DisplayFor(modelItem => item.ProductionStage.Name)
                                            </a>
                                        }
                                        else
                                        {
                                            <a class="btn disabled" type="button" style="padding-bottom:2px;color:white;background:@Html.DisplayFor(modelItem => item.ProductionStage.Color)">
                                                @Html.DisplayFor(modelItem => item.ProductionStage.Name)
                                            </a>
                                        }
                                    }
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Client.Name)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.OrderName)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Product.Name)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Quantity)
                                </td>
                                <td>
                                    <a class="btn btn-outline-success" asp-action="Edit" asp-route-id="@item.OrderID" role="button" data-toggle="tooltip" title="Edytuj">
                                        <i class="fe fe-edit fe-16"></i>
                                    </a>
                                    <a class="btn btn-outline-dark" style="margin-bottom:1px" asp-action="ChangeProductionStage" asp-route-id="@item.OrderID" asp-route-prodId="8" role="button" data-toggle="tooltip" title="Archiwum">
                                        <i class="fe fe-archive fe-16"></i>
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>